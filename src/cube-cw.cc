#include <iostream>

#include <cube.h>

using namespace std;

int main(int argc, char* argv[] ) {
    cout << "Computer graphic. Control work." << endl << endl;

    cube(argc, argv);

    exit(EXIT_SUCCESS);
}
